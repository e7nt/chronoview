/**
 * Runtime Configuration
 * Generated at container startup by entrypoint.sh using envsubst.
 * In local dev, falls back to import.meta.env via the Vite proxy.
 */
window.__APP_CONFIG__ = {
  VITE_API_URL: '${VITE_API_URL}'
};
